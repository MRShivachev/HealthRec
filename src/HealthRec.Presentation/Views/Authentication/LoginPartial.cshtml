@*@using Microsoft.AspNetCore.Identity
@using HealthRec.Data.Entities
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager

<ul class="navbar-nav">
    @if (SignInManager.IsSignedIn(User))
    {
        var user = await UserManager.GetUserAsync(User);
        var userType = user?.UserType.ToString() ?? "User";
        var iconClass = userType == "Doctor" ? "fas fa-user-md" : "fas fa-user";
        var textClass = userType == "Doctor" ? "text-primary" : "text-success";
    
        <li class="nav-item">
            <span class="nav-link text-dark">
                <i class="@iconClass @textClass me-1"></i>Hello @(user?.FirstName ?? User.Identity?.Name)!
            </span>
        </li>
        <li class="nav-item">
            <form class="form-inline" asp-area="" asp-controller="Account" asp-action="Logout">
                <button type="submit" class="nav-link btn btn-link text-dark">Logout</button>
            </form>
        </li>
    }
    else
    {
        <li class="nav-item">
            <a class="nav-link text-dark" asp-area="" asp-controller="Account" asp-action="LoginOptions">
                <i class="fas fa-sign-in-alt me-1"></i>Login/Register
            </a>
        </li>
    }
</ul>*@